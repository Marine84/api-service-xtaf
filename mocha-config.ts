// // import Mocha from "mocha";
// // @ts-ignore
// const Mocha = require("mocha");
//
// let mochaMain = new Mocha({
//     reporter: 'report-portal-reporter'
// });
//
//
// // run tests
// try{
//     mochaMain.files = ["./specs/basketball.spec.ts"];
//     mochaMain.run((failures:any) => process.on('exit', () => process.exit(failures))); // exit with non-zero exit code, other wise fails will not fail mocha run
// } catch (err) {
//     console.error(`Test suite doesn't exists or set. Error: ${err}`);
// }
